-- public.categorias definition
 
 
-- Drop table
 
 
-- DROP TABLE categorias;
 
 
CREATE TABLE categorias (
    id serial4 NOT NULL,
    nombre varchar(100) NOT NULL,
    descripcion text NULL,
    is_active bool DEFAULT true NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    updated_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    CONSTRAINT categorias_pkey PRIMARY KEY (id)
);
 
 
 
 
-- public.clientes definition
 
 
-- Drop table
 
 
-- DROP TABLE clientes;
 
 
CREATE TABLE clientes (
    id serial4 NOT NULL,
    nombres varchar(150) NOT NULL,
    telefono varchar(20) NOT NULL,
    direccion text NULL,
    is_active bool DEFAULT true NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    updated_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    correo varchar(100) NOT NULL,
    documento varchar(50) NULL,
    apellidos varchar(150) DEFAULT ''::character varying NULL,
    password_hash varchar(255) NULL,
    reset_password_token varchar(255) NULL,
    reset_password_expires timestamp NULL,
    CONSTRAINT clientes_correo_key UNIQUE (correo),
    CONSTRAINT clientes_pkey PRIMARY KEY (id)
);
 
 
 
 
-- public.permisos definition
 
 
-- Drop table
 
 
-- DROP TABLE permisos;
 
 
CREATE TABLE permisos (
    id serial4 NOT NULL,
    nombre varchar(50) NOT NULL,
    descripcion varchar(150) NOT NULL,
    CONSTRAINT permisos_nombre_key UNIQUE (nombre),
    CONSTRAINT permisos_pkey PRIMARY KEY (id)
);
 
 
 
 
-- public.proveedores definition
 
 
-- Drop table
 
 
-- DROP TABLE proveedores;
 
 
CREATE TABLE proveedores (
    id serial4 NOT NULL,
    nombre varchar(150) NOT NULL,
    contacto varchar(150) NULL,
    email varchar(100) NULL,
    telefono varchar(20) NULL,
    direccion text NULL,
    is_active bool DEFAULT true NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    nit varchar(50) NULL,
    ciudad varchar(100) NULL,
    descripcion text NULL,
    CONSTRAINT proveedores_pkey PRIMARY KEY (id)
);
 
 
 
 
-- public.roles definition
 
 
-- Drop table
 
 
-- DROP TABLE roles;
 
 
CREATE TABLE roles (
    id serial4 NOT NULL,
    nombre varchar(50) NOT NULL,
    descripcion text NULL,
    is_active bool DEFAULT true NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    CONSTRAINT roles_nombre_key UNIQUE (nombre),
    CONSTRAINT roles_pkey PRIMARY KEY (id)
);
 
 
 
 
-- public.compras definition
 
 
-- Drop table
 
 
-- DROP TABLE compras;
 
 
CREATE TABLE compras (
    id serial4 NOT NULL,
    proveedor_id int4 NOT NULL,
    total numeric(12, 2) DEFAULT 0.00 NOT NULL,
    estado varchar(30) DEFAULT 'Completado'::character varying NOT NULL,
    notas text NULL,
    fecha_compra timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    CONSTRAINT compras_pkey PRIMARY KEY (id),
    CONSTRAINT fk_compra_proveedor FOREIGN KEY (proveedor_id) REFERENCES proveedores(id) ON DELETE RESTRICT
);
 
 
 
 
-- public.productos definition
 
 
-- Drop table
 
 
-- DROP TABLE productos;
 
 
CREATE TABLE productos (
    id serial4 NOT NULL,
    categoria_id int4 NOT NULL,
    nombre varchar(150) NOT NULL,
    precio numeric(12, 2) NOT NULL,
    is_active bool DEFAULT true NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    updated_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    genero varchar(20) DEFAULT 'Unisex'::character varying NULL,
    CONSTRAINT productos_pkey PRIMARY KEY (id),
    CONSTRAINT productos_precio_check CHECK ((precio >= (0)::numeric)),
    CONSTRAINT fk_producto_categoria FOREIGN KEY (categoria_id) REFERENCES categorias(id) ON DELETE RESTRICT
);
 
 
 
 
-- public.rol_permisos definition
 
 
-- Drop table
 
 
-- DROP TABLE rol_permisos;
 
 
CREATE TABLE rol_permisos (
    id serial4 NOT NULL,
    rol_id int4 NULL,
    permiso_id int4 NULL,
    CONSTRAINT rol_permisos_pkey PRIMARY KEY (id),
    CONSTRAINT rol_permisos_rol_id_permiso_id_key UNIQUE (rol_id, permiso_id),
    CONSTRAINT rol_permisos_permiso_id_fkey FOREIGN KEY (permiso_id) REFERENCES permisos(id) ON DELETE CASCADE,
    CONSTRAINT rol_permisos_rol_id_fkey FOREIGN KEY (rol_id) REFERENCES roles(id) ON DELETE CASCADE
);
 
 
 
 
-- public.usuarios definition
 
 
-- Drop table
 
 
-- DROP TABLE usuarios;
 
 
CREATE TABLE usuarios (
    id serial4 NOT NULL,
    rol_id int4 NOT NULL,
    nombre varchar(100) NOT NULL,
    email varchar(100) NOT NULL,
    password_hash varchar(255) NOT NULL,
    is_active bool DEFAULT true NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    updated_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    reset_password_token varchar(255) NULL,
    reset_password_expires timestamp NULL,
    CONSTRAINT usuarios_email_key UNIQUE (email),
    CONSTRAINT usuarios_pkey PRIMARY KEY (id),
    CONSTRAINT fk_usuario_rol FOREIGN KEY (rol_id) REFERENCES roles(id) ON DELETE RESTRICT
);
 
 
 
 
-- public.ventas definition
 
 
-- Drop table
 
 
-- DROP TABLE ventas;
 
 
CREATE TABLE ventas (
    id serial4 NOT NULL,
    codigo_venta varchar(20) NOT NULL,
    cliente_id int4 NULL,
    usuario_id int4 NULL,
    total numeric(12, 2) DEFAULT 0.00 NOT NULL,
    estado varchar(30) DEFAULT 'Completado'::character varying NOT NULL,
    fecha_venta timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    origen varchar(30) DEFAULT 'Manual'::character varying NULL,
    pedido_id int4 NULL,
    motivo_anulacion text NULL,
    CONSTRAINT ventas_codigo_venta_key UNIQUE (codigo_venta),
    CONSTRAINT ventas_pkey PRIMARY KEY (id),
    CONSTRAINT fk_venta_cliente FOREIGN KEY (cliente_id) REFERENCES clientes(id) ON DELETE RESTRICT,
    CONSTRAINT fk_venta_usuario FOREIGN KEY (usuario_id) REFERENCES usuarios(id) ON DELETE RESTRICT
);
 
 
 
 
-- public.carrito definition
 
 
-- Drop table
 
 
-- DROP TABLE carrito;
 
 
CREATE TABLE carrito (
    id serial4 NOT NULL,
    cliente_id int4 NOT NULL,
    producto_id int4 NOT NULL,
    talla varchar(10) NOT NULL,
    cantidad int4 NOT NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    updated_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    CONSTRAINT carrito_cantidad_check CHECK ((cantidad > 0)),
    CONSTRAINT carrito_cliente_id_producto_id_talla_key UNIQUE (cliente_id, producto_id, talla),
    CONSTRAINT carrito_pkey PRIMARY KEY (id),
    CONSTRAINT fk_carrito_cliente FOREIGN KEY (cliente_id) REFERENCES clientes(id) ON DELETE CASCADE,
    CONSTRAINT fk_carrito_producto FOREIGN KEY (producto_id) REFERENCES productos(id) ON DELETE CASCADE
);
 
 
 
 
-- public.detalle_compras definition
 
 
-- Drop table
 
 
-- DROP TABLE detalle_compras;
 
 
CREATE TABLE detalle_compras (
    id serial4 NOT NULL,
    compra_id int4 NOT NULL,
    producto_id int4 NOT NULL,
    talla_comprada varchar(10) NOT NULL,
    cantidad int4 NOT NULL,
    precio_unitario numeric(12, 2) NOT NULL,
    subtotal numeric(12, 2) GENERATED ALWAYS AS ((cantidad::numeric * precio_unitario)) STORED NULL,
    precio_venta numeric(12, 2) DEFAULT 0.00 NULL,
    CONSTRAINT detalle_compras_cantidad_check CHECK ((cantidad > 0)),
    CONSTRAINT detalle_compras_pkey PRIMARY KEY (id),
    CONSTRAINT fk_detalle_compra FOREIGN KEY (compra_id) REFERENCES compras(id) ON DELETE CASCADE,
    CONSTRAINT fk_detalle_compra_producto FOREIGN KEY (producto_id) REFERENCES productos(id) ON DELETE RESTRICT
);
 
 
 
 
-- public.detalle_ventas definition
 
 
-- Drop table
 
 
-- DROP TABLE detalle_ventas;
 
 
CREATE TABLE detalle_ventas (
    id serial4 NOT NULL,
    venta_id int4 NOT NULL,
    producto_id int4 NOT NULL,
    talla_vendida varchar(10) NOT NULL,
    cantidad int4 NOT NULL,
    precio_unitario numeric(12, 2) NOT NULL,
    subtotal numeric(12, 2) GENERATED ALWAYS AS ((cantidad::numeric * precio_unitario)) STORED NULL,
    CONSTRAINT detalle_ventas_cantidad_check CHECK ((cantidad > 0)),
    CONSTRAINT detalle_ventas_pkey PRIMARY KEY (id),
    CONSTRAINT fk_detalle_producto FOREIGN KEY (producto_id) REFERENCES productos(id) ON DELETE RESTRICT,
    CONSTRAINT fk_detalle_venta FOREIGN KEY (venta_id) REFERENCES ventas(id) ON DELETE CASCADE
);
 
 
 
 
-- public.pedidos definition
 
 
-- Drop table
 
 
-- DROP TABLE pedidos;
 
 
CREATE TABLE pedidos (
    id serial4 NOT NULL,
    codigo_pedido varchar(20) NOT NULL,
    cliente_id int4 NULL,
    usuario_id int4 NULL,
    nombre_cliente varchar(200) NOT NULL,
    telefono varchar(20) NULL,
    direccion_entrega text NULL,
    fecha_entrega date NULL,
    observaciones text NULL,
    estado varchar(30) DEFAULT 'Pendiente'::character varying NOT NULL,
    total numeric(12, 2) DEFAULT 0.00 NOT NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    motivo_anulacion text NULL,
    CONSTRAINT pedidos_codigo_pedido_key UNIQUE (codigo_pedido),
    CONSTRAINT pedidos_pkey PRIMARY KEY (id),
    CONSTRAINT fk_pedido_cliente FOREIGN KEY (cliente_id) REFERENCES clientes(id) ON DELETE SET NULL,
    CONSTRAINT fk_pedido_usuario FOREIGN KEY (usuario_id) REFERENCES usuarios(id) ON DELETE SET NULL
);
 
 
 
 
-- public.producto_imagenes definition
 
 
-- Drop table
 
 
-- DROP TABLE producto_imagenes;
 
 
CREATE TABLE producto_imagenes (
    id serial4 NOT NULL,
    producto_id int4 NOT NULL,
    url_imagen text NOT NULL,
    es_principal bool DEFAULT false NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    CONSTRAINT producto_imagenes_pkey PRIMARY KEY (id),
    CONSTRAINT fk_imagen_producto FOREIGN KEY (producto_id) REFERENCES productos(id) ON DELETE CASCADE
);
 
 
 
 
-- public.producto_tallas definition
 
 
-- Drop table
 
 
-- DROP TABLE producto_tallas;
 
 
CREATE TABLE producto_tallas (
    id serial4 NOT NULL,
    producto_id int4 NOT NULL,
    talla varchar(10) NOT NULL,
    stock int4 DEFAULT 0 NOT NULL,
    created_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    updated_at timestamp DEFAULT CURRENT_TIMESTAMP NULL,
    CONSTRAINT producto_tallas_pkey PRIMARY KEY (id),
    CONSTRAINT producto_tallas_producto_id_talla_key UNIQUE (producto_id, talla),
    CONSTRAINT producto_tallas_stock_check CHECK ((stock >= 0)),
    CONSTRAINT fk_talla_producto FOREIGN KEY (producto_id) REFERENCES productos(id) ON DELETE CASCADE
);
 
 
 
 
-- public.detalle_pedidos definition
 
 
-- Drop table
 
 
-- DROP TABLE detalle_pedidos;
 
 
CREATE TABLE detalle_pedidos (
    id serial4 NOT NULL,
    pedido_id int4 NOT NULL,
    producto_id int4 NOT NULL,
    talla varchar(10) NOT NULL,
    cantidad int4 NOT NULL,
    precio_unitario numeric(12, 2) NOT NULL,
    subtotal numeric(12, 2) GENERATED ALWAYS AS ((cantidad::numeric * precio_unitario)) STORED NULL,
    CONSTRAINT detalle_pedidos_cantidad_check CHECK ((cantidad > 0)),
    CONSTRAINT detalle_pedidos_pkey PRIMARY KEY (id),
    CONSTRAINT fk_detalle_pedido FOREIGN KEY (pedido_id) REFERENCES pedidos(id) ON DELETE CASCADE,
    CONSTRAINT fk_detalle_pedido_producto FOREIGN KEY (producto_id) REFERENCES productos(id) ON DELETE RESTRICT
);